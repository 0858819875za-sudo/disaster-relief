# หลักฐานการทดสอบ (Test Evidence)

เก็บ screenshot **ก่อน–หลัง** ของทุก Test Case ตาม `docs/Week10_Topic8_Analysis.md` ข้อ 8
ตั้งชื่อไฟล์ `TCxx-before.png` / `TCxx-after.png` (หรือวิดีโอสั้น `TCxx.mp4`)

## F5 — จัดสรรและตัดจ่าย

ต้องรัน `docs/sql/17_f5_hardening.sql` และ `docs/sql/18_f5_features.sql` ใน Supabase ก่อนทดสอบ

### เตรียมข้อมูลและลำดับการเก็บหลักฐาน

1. รัน `docs/sql/22_tc_f5_setup.sql` — ได้คำขอ `[TEST] TC ข้าวสาร 5 กก.` 50 ถุง (ศูนย์พักพิงวัดคลองแห)
   และล็อต A 100 / ล็อต B 20 / ล็อต C หมดอายุ 40 (ศูนย์รับบริจาคหาดใหญ่)
2. ทำตามลำดับนี้ เพราะแต่ละเคสต่อยอดจากเคสก่อน (ล็อกอินเป็น admin ทั้งหมด)
   - **TC09** เปิดคำขอในหน้าจัดสรร → ล็อต C ต้องไม่อยู่ในตัวเลือก
   - **TC08** ใส่ 50 ในล็อต B (เหลือ 20) → ปุ่มยืนยันกดไม่ได้ ยอดไม่เปลี่ยน
   - **TC07** จ่าย 30 จากล็อต A → ล็อต A เหลือ 70, คำขอ `partial`
   - **TC11** ยกเลิกรายการ 30 นั้นในหน้าประวัติ (กรอกเหตุผล) → ล็อต A กลับเป็น 100, คำขอ `pending`
   - **TC10** จ่าย 30 จากล็อต A อีกครั้ง แล้วจ่ายส่วนที่เหลือ 20 → คำขอ `fulfilled`
3. เก็บหลักฐานเสร็จแล้ว ยกเลิกรายการจัดสรรที่ค้าง (ถ้ามี) แล้วรัน `docs/sql/20_cleanup_test_records.sql`

| # | สิ่งที่ทดสอบ | บัญชี | ขั้นตอน | ผลที่คาดหวัง | หลักฐาน |
|---|---|---|---|---|---|
| TC07 | จัดสรรบางส่วน | admin | คำขอ 50 → จ่าย 30 จากล็อตที่มี 100 | ล็อตคงเหลือ 70, คำขอ `partial` | ☐ |
| TC08 | จ่ายเกินยอดคงเหลือ | admin | ล็อตเหลือ 20 แต่สั่งจ่าย 50 | Modal ไม่ให้กดยืนยัน + ถ้าเรียก rpc ตรง DB ปฏิเสธ ยอดไม่เปลี่ยน | ☐ |
| TC09 | จ่ายของหมดอายุ | admin | ล็อต `expiry_date` < วันนี้ | ล็อตไม่ขึ้นในตัวเลือก + DB ปฏิเสธพร้อมเหตุผล | ☐ |
| TC10 | จ่ายครบตามคำขอ | admin | จ่ายส่วนที่เหลืออีก 20 | คำขอเปลี่ยนเป็น `fulfilled` อัตโนมัติ | ☐ |
| TC11 | ยกเลิกแล้วคืนยอด | admin | ยกเลิก allocation 30 ชิ้น | ถามยืนยันก่อน → ล็อตกลับเป็น 100, คำขอกลับเป็น `pending` | ☐ |

### เคสสิทธิ์เพิ่มเติม (แนะนำ)

| สิ่งที่ทดสอบ | บัญชี | ผลที่คาดหวัง |
|---|---|---|
| staff จัดสรรข้ามศูนย์ | staff | ไม่มีคำขอ/ล็อตศูนย์อื่นให้เลือก และ DB ปฏิเสธ `cross_center_admin_only` |
| อาสาสมัครเรียก `allocate_items` ตรง | volunteer | DB ปฏิเสธ `not_staff` |
| ยืนยันส่งมอบของศูนย์อื่น | staff/volunteer | DB ปฏิเสธ `not_your_center` |
| สลับภาษาเป็น EN แล้วทำให้เกิด error | ใดก็ได้ | ป๊อปอัป error เป็นภาษาอังกฤษ |

### ฟีเจอร์เพิ่มของ F5

| สิ่งที่ทดสอบ | ขั้นตอน | ผลที่คาดหวัง |
|---|---|---|
| จับคู่อัตโนมัติ (FEFO) | เลือกคำขอที่มีหลายล็อต | ล็อตใกล้หมดอายุอยู่บนสุด และจำนวนถูกเติมไล่จากล็อตแรกจนครบที่ขาด |
| จัดสรรหลายล็อต | ใส่จำนวนใน 2 ล็อตแล้วยืนยัน | ป๊อปอัปสำเร็จแสดงทั้ง 2 ล็อต, ประวัติมี 2 แถว |
| หลายล็อต rollback | ตั้งให้ล็อตที่ 2 เกินคงเหลือ (ผ่าน rpc ตรง) | ถูกปฏิเสธทั้งชุด ล็อตที่ 1 ยอดไม่เปลี่ยน |
| ยกเลิกต้องมีเหตุผล | admin กดยกเลิกแล้วกรอกเหตุผล | ยอดคืน, ประวัติแสดงเหตุผลและชื่อคนยกเลิก |
| ตัวกรองประวัติ | กรองสถานะ/ศูนย์/ช่วงวันที่ | แสดงเฉพาะแถวที่ตรงเงื่อนไข |
| ใบส่งมอบ | กดไอคอนเครื่องพิมพ์ในประวัติ | หน้าใบส่งมอบแสดงข้อมูลครบ พิมพ์ได้ |
